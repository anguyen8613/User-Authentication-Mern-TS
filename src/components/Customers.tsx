import { FC } from 'react';

const Customers: FC = () => {
  return (
    <div>
      <h5 className='text-center'>
        Customers component is now available since the user is logged in.
      </h5>
    </div>
  );
};

export default Customers;
